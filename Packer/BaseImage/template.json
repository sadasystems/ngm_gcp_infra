{

  "variables": {
    "env_user": "{{env `USER`}}",
    "project_id": "{{env `TF_VAR_project_id`}}",
    "zone": "{{env `TF_VAR_zone`}}"
  },
  "builders": [
    {
      "type": "googlecompute",
      "project_id": "{{user `project_id`}}",
      "source_image_family": "ubuntu-1404-lts",
      "ssh_username": "packer",
      "zone": "{{user `zone`}}",
      "image_name": "base-image-{{isotime \"2006-01-02 03_04_05\" | clean_image_name }}",
      "image_description": "A base image.",
      "image_family": "base-image-1404-lts",
      "omit_external_ip": false
    }
  ]
}
